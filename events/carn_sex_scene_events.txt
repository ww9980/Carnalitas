namespace = carn_sex_scene

#
# 9999. Fallback event for when no scene matches the provided parameters.
# This simply adds the consensual flag and fires the paradox sex scene
#

carn_sex_scene.9999 = {
	type = empty
	hidden = yes

	immediate = {
		carn_sex_scene_set_consensual = yes
		trigger_event = { carn_sex_scene.0001 }
	}
}